<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- TODO: Add image for home screen logo. -->
        <ImageView
            android:id="@+id/iv_home_logo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:importantForAccessibility="no"
            android:src="@drawable/ballot_logo"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- TODO: Add Button to navigate to upcoming elections. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/mbt_upcoming_elections"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/default_margin_screen"
            android:layout_marginEnd="@dimen/default_margin_screen"
            android:layout_marginBottom="16dp"
            android:text="upcoming elections"
            app:layout_constraintBottom_toTopOf="@id/mbt_find_my_representatives"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- TODO: Add Button to navigate to representatives. -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/mbt_find_my_representatives"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/default_margin_screen"
            android:layout_marginEnd="@dimen/default_margin_screen"
            android:layout_marginBottom="32dp"
            android:text="find my representatives"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>